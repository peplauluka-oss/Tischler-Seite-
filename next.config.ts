import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Alle Seiten sind statisch generierbar (SSG) – gut für SEO & Performance.
  reactStrictMode: true,
};

export default nextConfig;
