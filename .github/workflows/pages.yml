name: Deploy GitHub Pages (Preview)

on:
  push:
    # Veröffentlicht wird ausschließlich über den Standard-Branch: Die
    # github-pages-Umgebung lässt Deployments nur von dort zu. Dessen
    # Workflow checkt diesen Branch aus. Nach Änderungen hier also den
    # Workflow "Deploy GitHub Pages" manuell starten (workflow_dispatch).
    branches: [main, claude/hobbytischlerei-relaunch-b6dpp9]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # Der Medusa-Prototyp hat kein Backend; falls ein Branch API-Routen
      # mitbringt, können sie im statischen Export nicht mitgebaut werden.
      - name: API-Routen für statischen Export entfernen
        run: rm -rf src/app/api
      - name: Build (statischer Export)
        run: npx next build
        env:
          GITHUB_PAGES: "true"
          BASE_PATH: /${{ github.event.repository.name }}
          NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
